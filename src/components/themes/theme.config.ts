/**
 * Default theme that loads when no user preference is set
 * Fynn uses WhatsApp theme exclusively
 */
export const DEFAULT_THEME = 'whatsapp';

export const THEMES = [
  {
    name: 'WhatsApp',
    value: 'whatsapp'
  }
];
